const startCommand = require('./start');
const statsCommand = require('./stats');
const locationCommand = require('./location');

module.exports = {
  startCommand,
  statsCommand,
  locationCommand,
};
