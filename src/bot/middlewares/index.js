const { cbQueryMiddleware } = require('./callbackQueryMiddleware');
const { settingsMenuMiddleware } = require('./settingsMenuMiddleware');

module.exports = { cbQueryMiddleware, settingsMenuMiddleware };
